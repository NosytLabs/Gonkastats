import {apiDefinitions} from './api-definitions';
export function openapi(){return {openapi:'3.1.0',info:{title:'GonkaStats read-only API',version:'0.1.0',description:'Independent Gonka observations. Numeric ledger quantities use decimal strings. No signing, payments, private accounts or arbitrary upstream proxy.'},servers:[{url:'/api/v1'}],tags:[...new Set(apiDefinitions.map(d=>d.group))].map(name=>({name})),paths:Object.fromEntries(apiDefinitions.map(d=>['/'+d.id,{get:{operationId:d.id.replaceAll('-','_'),tags:[d.group],summary:d.title,description:d.description+' '+d.cache,parameters:d.parameters.map(p=>({name:p.name,in:'query',required:p.required??false,description:p.description,schema:{type:p.type??'string',...(p.enum?{enum:p.enum}:{}),...(p.min!==undefined?{minimum:p.min}:{}),...(p.max!==undefined?{maximum:p.max}:{}),...(p.default!==undefined?{default:p.type==='integer'?Number(p.default):p.default}:{})}})),responses:{200:{description:'Observed data with provenance; openapi returns this specification directly.',content:{'application/json':{schema:{$ref:'#/components/schemas/Observation'}}}},400:{description:'Invalid or unsupported parameter'},404:{description:'Resource or model not found'},429:{description:'Process read budget reached; retry after the indicated delay'},502:{description:'Upstream lookup failed'},503:{description:'Data source or optional integration unavailable'}}}}])),components:{schemas:{Observation:{type:'object',properties:{data:{description:'Endpoint-specific payload. Null or empty values require inspection of source availability and coverage.'},meta:{type:'object',properties:{schemaVersion:{type:'string'},generatedAt:{type:'string',format:'date-time'},mode:{enum:['live','snapshot']},sources:{type:'array',items:{$ref:'#/components/schemas/Source'}},pagination:{type:'object',properties:{offset:{type:'integer'},limit:{type:'integer'},returned:{type:'integer'},retainedMatches:{type:'integer'}}}}}}},Source:{type:'object',required:['id','url','status','fetchedAt','scope','coverage'],properties:{id:{type:'string'},url:{type:'string',format:'uri'},status:{enum:['recent','stale','snapshot','unavailable']},fetchedAt:{type:'string',format:'date-time'},sourceTime:{type:['string','null']},scope:{enum:['chain','indexer','provider','estimate']},coverage:{type:'string'},error:{type:['string','null']}}}}}};}
